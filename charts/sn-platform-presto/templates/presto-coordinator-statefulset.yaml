#
# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
#

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ template "presto.coordinator" . }}
  namespace: {{ template "pulsar.namespace" . }}
  labels:
    {{- include "pulsar.standardLabels" . | nindent 4 }}
    component: {{ .Values.coordinator.component }}
spec:
  serviceName: {{ template "presto.service" . }}
  replicas: {{ .Values.coordinator.replicaCount }}
  selector:
    matchLabels:
      {{- include "pulsar.matchLabels" . | nindent 6 }}
      component: {{ .Values.coordinator.component }}
  updateStrategy:
    type: RollingUpdate
  podManagementPolicy: Parallel
  template:
    metadata:
      labels:
        {{- include "pulsar.template.labels" . | nindent 8 }}
        component: {{ .Values.coordinator.component }}
      annotations:
        prometheus.io/scrape: "false"
        prometheus.io/port: "{{ .Values.coordinator.ports.http }}"
        {{- if .Values.autoRollDeployment }}
        checksum/config: {{ include (print $.Template.BasePath "/presto-coordinator-configmap.yaml") . | sha256sum }}
        {{- end }}
        {{- include "pulsar.istio.pod.annotations" . | nindent 8 }}
{{- with .Values.coordinator.annotations }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
      {{- with .Values.coordinator.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.coordinator.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
      {{- end }}
      affinity:
      {{- if and .Values.affinity.anti_affinity .Values.coordinator.affinity.anti_affinity}}
        podAntiAffinity:
          {{ .Values.coordinator.affinity.type }}:
          {{ if eq .Values.coordinator.affinity.type "requiredDuringSchedulingIgnoredDuringExecution"}}
          - labelSelector:
              matchExpressions:
              - key: "app"
                operator: In
                values:
                - "{{ template "pulsar.name" . }}"
              - key: "release"
                operator: In
                values:
                - {{ .Release.Name }}
              - key: "component"
                operator: In
                values:
                - {{ .Values.coordinator.component }}
            topologyKey: "kubernetes.io/hostname"
          {{ else }}
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: "app"
                      operator: In
                      values:
                      - "{{ template "pulsar.name" . }}"
                    - key: "release"
                      operator: In
                      values:
                      - {{ .Release.Name }}
                    - key: "component"
                      operator: In
                      values:
                      - {{ .Values.coordinator.component }}
                topologyKey: "kubernetes.io/hostname"
          {{ end }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.coordinator.gracePeriod }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}-coordinator
          image: "{{ .Values.images.presto.repository }}:{{ .Values.images.presto.tag }}"
          imagePullPolicy: {{ .Values.images.presto.pullPolicy }}
          {{- if .Values.coordinator.probe.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.coordinator.probe.liveness.path }}
              port: {{ .Values.coordinator.ports.http }}
            initialDelaySeconds: {{ .Values.coordinator.probe.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.coordinator.probe.liveness.periodSeconds }}
            failureThreshold: {{ .Values.coordinator.probe.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.coordinator.probe.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.coordinator.probe.readiness.path }}
              port: {{ .Values.coordinator.ports.http }}
            initialDelaySeconds: {{ .Values.coordinator.probe.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.coordinator.probe.readiness.periodSeconds }}
            failureThreshold: {{ .Values.coordinator.probe.readiness.failureThreshold }}
          {{- end }}
          {{- if .Values.coordinator.resources }}
          resources:
{{ toYaml .Values.coordinator.resources | indent 12 }}
          {{- end }}
          command: ["sh", "-c"]
          args:
            - >-
              cp {{ template "pulsar.home" . }}/conf/presto/node.properties.template {{ template "pulsar.home" . }}/conf/presto/node.properties;
              echo "node.id=${HOSTNAME}" >> {{ template "pulsar.home" . }}/conf/presto/node.properties ;
              echo "node.internal-address=${HOSTNAME}.{{ template "presto.service" . }}.{{ template "pulsar.namespace" . }}.svc.cluster.local" >> {{ template "pulsar.home" . }}/conf/presto/node.properties ;
              ln -s {{ template "pulsar.home" . }}/conf/presto {{ template "pulsar.home" . }}/lib/presto/etc;
              bin/pulsar sql-worker run \
                --etc-dir={{ template "pulsar.home" . }}/conf/presto \
                --data-dir={{ template "pulsar.home" . }}/data;
          env:
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
{{- with .Values.coordinator.extraEnv }}
{{- toYaml . | nindent 10 }}
{{- end }}      
          volumeMounts:
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/node.properties.template
              name: config-volume
              subPath: node.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/log.properties
              name: config-volume
              subPath: log.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/jvm.config
              name: config-volume
              subPath: jvm.config
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/config.properties
              name: config-volume
              subPath: config.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/catalog/pulsar.properties
              name: config-volume
              subPath: pulsar.properties
            {{- if or .Values.security.authentication.jwt.enabled .Values.security.authentication.password.enabled }}
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/rules.json
              name: config-volume
              subPath: rules.json
            {{- end}}
            {{- if .Values.security.authentication.password.enabled }}
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/password-authenticator.properties
              name: config-volume
              subPath: password-authenticator.properties
            - mountPath: {{ template "pulsar.home" . }}/presto
              name: password-file-volume
            {{- end}}
            {{- if .Values.security.authentication.jwt.enabled }}
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/access-control.properties
              name: config-volume
              subPath: access-control.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/{{ .Values.security.authentication.jwt.publicKeyFileName }}
              name: public-key-volume
              subPath: {{ .Values.security.authentication.jwt.publicKeyConfigMapKey  }}
            {{- end}}
{{- with .Values.coordinator.extraVolumeMounts }}
{{- toYaml . | nindent 12 }}
{{- end }}
          ports:
{{- if .Values.tls.presto.enabled }}
            - name: https
              containerPort: {{ .Values.coordinator.ports.https }}
              protocol: TCP
{{- else }}
            - name: http
              containerPort: {{ .Values.coordinator.ports.http }}
              protocol: TCP
{{- end }}
      volumes:
        - name: config-volume
          configMap:
            name: "{{ template "pulsar.fullname" . }}-{{ .Values.coordinator.component }}"
        {{- if .Values.security.authentication.jwt.enabled }}
        - name: public-key-volume
          configMap:
            name: {{ .Values.security.authentication.jwt.publicKeyConfigMapName  }}
        {{- end}}
        {{- if .Values.security.authentication.password.enabled }}
        - name: password-file-volume
          secret:
            secretName: {{ .Values.security.authentication.password.passwordFileSecret }}
            items:
            - key: {{ .Values.security.authentication.password.passwordFileSecretKey }}
              path: {{ .Values.security.authentication.password.passwordFileName }}
        {{- end}}
{{- with .Values.coordinator.extraVolumes }}
{{- toYaml . | nindent 8 }}
{{- end }}
