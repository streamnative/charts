#
# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
#
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ template "presto.worker" . }}
  namespace: {{ template "pulsar.namespace" . }}
  labels:
    {{- include "pulsar.standardLabels" . | nindent 4 }}
    component: {{ .Values.worker.component }}
spec:
  serviceName: {{ template "presto.worker.service" . }}
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "pulsar.matchLabels" . | nindent 6 }}
      component: {{ .Values.worker.component }}
  updateStrategy:
    type: RollingUpdate
  podManagementPolicy: Parallel
  template:
    metadata:
      labels:
        {{- include "pulsar.template.labels" . | nindent 8 }}
        component: {{ .Values.worker.component }}
      annotations:
        prometheus.io/scrape: "false"
        prometheus.io/port: "{{ .Values.worker.ports.http }}"
        {{- if .Values.autoRollDeployment }}
        checksum/config: {{ include (print $.Template.BasePath "/presto-worker-configmap.yaml") . | sha256sum }}
        {{- end }}
        {{- include "pulsar.istio.pod.annotations" . | nindent 8 }}
{{- with .Values.worker.annotations }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
      {{- with .Values.worker.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.worker.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
      {{- end }}
      affinity:
      {{- if and .Values.affinity.anti_affinity .Values.worker.affinity.anti_affinity}}
        podAntiAffinity:
          {{ .Values.worker.affinity.type }}:
          {{ if eq .Values.worker.affinity.type "requiredDuringSchedulingIgnoredDuringExecution"}}
          - labelSelector:
              matchExpressions:
              - key: "app"
                operator: In
                values:
                - "{{ template "pulsar.name" . }}"
              - key: "release"
                operator: In
                values:
                - {{ .Release.Name }}
              - key: "component"
                operator: In
                values:
                - {{ .Values.worker.component }}
            topologyKey: "kubernetes.io/hostname"
          {{ else }}
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: "app"
                      operator: In
                      values:
                      - "{{ template "pulsar.name" . }}"
                    - key: "release"
                      operator: In
                      values:
                      - {{ .Release.Name }}
                    - key: "component" 
                      operator: In
                      values:
                      - {{ .Values.worker.component }}
                topologyKey: "kubernetes.io/hostname"
          {{ end }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.worker.gracePeriod }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}-worker
          {{- include "presto.worker.image" . | nindent 10 }}
          {{- if .Values.worker.probe.liveness.enabled }}
          livenessProbe:
            exec:
              command:
                - /bin/bash
                - /presto/health_check.sh
            initialDelaySeconds: {{ .Values.worker.probe.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.probe.liveness.periodSeconds }}
            failureThreshold: {{ .Values.worker.probe.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.worker.probe.readiness.enabled }}
          readinessProbe:
            exec:
              command:
                - /bin/bash
                - /presto/health_check.sh
            initialDelaySeconds: {{ .Values.worker.probe.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.probe.readiness.periodSeconds }}
            failureThreshold: {{ .Values.worker.probe.readiness.failureThreshold }}
          {{- end }}
          {{- if .Values.worker.resources }}
          resources:
{{ toYaml .Values.worker.resources | indent 12 }}
          {{- end }}
          command: ["sh", "-c"]
          args:
            - >-
              cp {{ template "pulsar.home" . }}/conf/presto/node.properties.template {{ template "pulsar.home" . }}/conf/presto/node.properties;
              echo "node.id=${HOSTNAME}" >> {{ template "pulsar.home" . }}/conf/presto/node.properties ;
              echo "node.internal-address=${HOSTNAME}.{{ template "presto.worker.service" . }}.{{ template "pulsar.namespace" . }}.svc.cluster.local" >> {{ template "pulsar.home" . }}/conf/presto/node.properties ;
              ln -s {{ template "pulsar.home" . }}/conf/presto {{ template "pulsar.home" . }}/lib/presto/etc;
              bin/pulsar sql-worker run \
                --etc-dir={{ template "pulsar.home" . }}/conf/presto \
                --data-dir={{ template "pulsar.home" . }}/data;
          env:
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
{{- with .Values.worker.extraEnv }}
{{- toYaml . | nindent 10 }}
{{- end }}
          volumeMounts:
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/node.properties.template
              name: config-volume
              subPath: node.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/log.properties
              name: config-volume
              subPath: log.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/jvm.config
              name: config-volume
              subPath: jvm.config
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/config.properties
              name: config-volume
              subPath: config.properties
            - mountPath: {{ template "pulsar.home" . }}/conf/presto/catalog/pulsar.properties
              name: config-volume
              subPath: pulsar.properties
            - mountPath: /presto/health_check.sh
              name: config-volume
              subPath: health_check.sh
{{- with .Values.worker.extraVolumeMounts }}
{{- toYaml . | nindent 12 }}
{{- end }}            
          ports:
            - name: http
              containerPort: {{ .Values.worker.ports.http }}
              protocol: TCP
      volumes:
        - name: config-volume
          configMap:
            name: "{{ template "pulsar.fullname" . }}-{{ .Values.worker.component }}"
{{- with .Values.worker.extraVolumes }}
{{- toYaml . | nindent 8 }}
{{- end }}
